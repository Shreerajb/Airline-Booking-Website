export interface Flight {
    flightNumber: string ,
    from: string,
    to: string,
    departureTime: Date,
    arrivalTime: Date,
    duration: string,
    price: number,
    seats: number
    
}